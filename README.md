# XiaoMeili Updates

这是“小美丽”桌宠的稳定更新源。

- 当前稳定版本：V0.6.1.1
- 更新清单：`latest.json`
- 客户端只需绑定一次固定清单地址，后续版本沿用同一地址。
- 用户配置、模型、声音选择和日志不应随程序更新被覆盖。

> 发布新版本时，应先上传更新包并计算 SHA-256，确认可下载后，再更新 `latest.json`，避免客户端读到未完成的版本。
